this is my first time creating AI agent with tool orchestration using the very basic library and minimal code.
the schema is simple(Actually too simple)
it runs 2 agent in parallel via asyncio then concat them together. In fact, it should've the main Agent to analyse the result + it should've max_iteration instead of while True to prevent token burning.
But it's not a bad start for learning Agentic AI I guess

<img width="592" height="877" alt="image" src="https://github.com/user-attachments/assets/56bd1569-bf8a-4639-9ffb-d1a3d17fecd4" />
